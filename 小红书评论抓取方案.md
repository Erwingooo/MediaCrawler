# 小红书评论抓取与前端展示方案

## 1. 方案概述

本方案旨在基于现有的 `MediaCrawler` 项目，实现针对指定小红书帖子的评论抓取，并通过 Web 前端页面展示抓取进度和结果，支持表格文件下载。

方案的核心是不修改 `MediaCrawler` 现有核心代码，而是通过新增一个轻量级的 Web API 服务（`web_api.py`）来调用和封装原有的爬虫逻辑，并提供 WebSocket 接口实时推送抓取进度。

## 2. 技术架构

### 2.1 后端 (Python/FastAPI)
- **Web 框架**: 使用 `FastAPI` 构建 HTTP 和 WebSocket 服务。
- **爬虫集成**: 
  - 继承 `XiaoHongShuCrawler` 类，创建 `WebXiaoHongShuCrawler`。
  - 重写 `get_comments` 方法，在调用原有存储逻辑的同时，通过 WebSocket 将评论数据实时推送到前端。
  - 通过拦截或自定义 `logging.Handler`，将日志信息实时推送到前端展示进度。
- **配置管理**: 动态修改 `config` 模块的全局变量（如 `XHS_SPECIFIED_NOTE_URL_LIST`, `CRAWLER_TYPE`, `HEADLESS`）以控制爬虫行为。
- **数据存储**: 沿用原有的 CSV 存储逻辑，文件保存在 `data/xhs/csv/` 目录下，并提供 API 供前端下载。

### 2.2 前端 (HTML/JS)
- **技术栈**: 原生 HTML5, CSS3, JavaScript (ES6+)。
- **功能**:
  - **输入区**: 输入小红书帖子 URL。
  - **控制区**: 启动/停止抓取按钮。
  - **日志区**: 实时显示爬虫运行日志（如 "正在抓取第 X 页..."）。
  - **数据表格**: 实时渲染抓取到的评论（头像、昵称、内容、时间、点赞数）。
  - **下载区**: 抓取完成后提供 CSV 文件下载链接。

## 3. 详细功能逻辑

### 3.1 启动抓取流程
1. 用户在前端输入帖子 URL，点击“开始抓取”。
2. 前端通过 WebSocket 连接到后端 `/ws/crawl` 接口，并发送开始指令（包含 URL）。
3. 后端接收指令后：
   - 设置 `config.CRAWLER_TYPE = "detail"`。
   - 设置 `config.XHS_SPECIFIED_NOTE_URL_LIST = [url]`。
   - 设置 `config.HEADLESS = False` (若需手动登录，可视浏览器界面)。
   - 实例化 `WebXiaoHongShuCrawler` 并调用 `start()` 方法。

### 3.2 登录处理
- 若 `MediaCrawler` 检测到未登录或 Cookie 失效：
  - 会自动触发 `XiaoHongShuLogin` 逻辑。
  - 由于配置了 `HEADLESS = False`，浏览器窗口会弹出。
  - 用户在弹出的 Chrome 浏览器新标签页中完成扫码或短信登录。
  - 登录成功后，爬虫自动继续执行后续抓取任务。
  - 前端日志区会提示“请在浏览器中完成登录”。

### 3.3 进度与数据实时展示
- **日志**: 后端自定义 LogHandler 捕获 `tools.utils.logger` 的日志，通过 WebSocket 发送 `{"type": "log", "data": "日志内容"}` 到前端。
- **评论**: `WebXiaoHongShuCrawler` 在获取到评论数据后，通过 WebSocket 发送 `{"type": "comment", "data": comment_dict}` 到前端。
- 前端接收到数据后，动态插入 HTML 表格。

### 3.4 结果导出
- 抓取结束后，后端通过 WebSocket 发送 `{"type": "done", "file_path": "..."}`。
- 前端显示“下载 CSV”按钮，点击请求后端 `/download?file=...` 接口下载文件。

## 4. 目录结构变更

```
MediaCrawler/
├── public/                 # 新增前端资源目录
│   ├── index.html          # 主页面
│   ├── style.css           # 样式表
│   └── script.js           # 逻辑脚本
├── web_api.py              # 新增 Web 服务入口
├── 小红书评论抓取方案.md     # 本文档
└── ... (原有项目文件)
```

## 5. 接口设计

### HTTP API
- `GET /`: 返回 `public/index.html` 页面。
- `GET /download`: 参数 `file_path`，下载指定生成的 CSV 文件。

### WebSocket API
- `/ws/crawl`:
  - **Client Send**: `{"action": "start", "url": "..."}`
  - **Server Send**:
    - `{"type": "log", "message": "..."}`
    - `{"type": "comment", "data": {...}}`
    - `{"type": "done", "file_path": "..."}`
    - `{"type": "error", "message": "..."}`

## 6. 使用说明

1. 确保已安装项目依赖：`pip install -r requirements.txt` (包含 `fastapi`, `uvicorn`, `playwright`)。
2. 运行 Web 服务：
   ```bash
   python web_api.py
   ```
3. 浏览器访问 `http://localhost:8000`。
4. 输入帖子 URL，点击开始。
5. 如遇登录弹窗，请在浏览器中操作。
6. 等待抓取完成，点击下载表格。
